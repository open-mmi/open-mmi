# Mapping fixtures

Replace this file with `mappings.v1.json` when the profile becomes a candidate.
Each case supplies exact CAN bytes and expected canonical events/status paths.
The replay gate must cover every event and non-alias status output claimed by the
profile.
